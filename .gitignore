node_modules/
.env
*.log
.DS_Store

# Keep snapshot history in git so week-over-week deltas survive restarts/redeploys
# and so the static (Vercel) build has data to serve.
# (public/data/*.json is intentionally NOT ignored.)
